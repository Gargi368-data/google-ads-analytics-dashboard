Create table google_ads_weekly (
  week_start date,
  campaign text,
  ad_group text,
  impressions int8,
  clicks int8,
  cost numeric,
  conversions int8,
  device text,
  location text,
  channel text )
